package Java.Basic.customList;

public abstract class AbstractCustomList implements CustomList{
    public void add() {
        // ? 
    }
    public void update(int index, Object value) {
        // ? 
    }
    public void remove(int index) {
        // ? 
    }
    public void clear() {
        // ? 
    }
}
